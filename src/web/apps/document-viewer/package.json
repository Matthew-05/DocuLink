{
  "name": "@talliark/document-viewer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p ../../tsconfig.tools.json && node build.ts",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@talliark/shared": "*",
    "pdfjs-dist": "^4.10.38"
  }
}
